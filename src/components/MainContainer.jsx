import React from "react";
// we want navbar, about, contact, portfolio (main), footer and the resume imported as well

// specified that we want to export by default the main container
// const [currentPage, setCurrentPage] = useState('About');

// we have an if section where it's checking to see what the current page is - find this in the homework assignments - const render page = () => {

// then we want to have a page change handler

// then we return (with a function that shows what's the current page on the navbar + handle page change
// then the page is rendered
// there is a footer with a self-closing tag

// all of the above is contained within a div 
// );